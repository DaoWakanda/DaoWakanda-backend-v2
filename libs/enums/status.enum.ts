export enum TRIVIA_STATUS {
  ONGOING = 'ongoing',
  EXPIRED = 'expired',
}

export enum SUBMISSION_STATUS {
  PENDING = 'pending',
  PASSED = 'passed',
  FAILED = 'failed',
}

export enum REVIEW_STATUS {
  PASSED = 'passed',
  FAILED = 'failed',
}
